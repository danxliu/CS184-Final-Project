# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# C++
build
.cache

# Submodules
ext/

# Generated demo output (frame sequences, CSVs, plots)
out/

# Procedurally generated assets (rebuilt by `./build/dump_test_meshes`)
assets/icosphere_*.obj
assets/torus.obj
assets/torus_12x8.obj
# External meshes not used by any current code path
assets/spot.obj

# Agent context (local-only working notes; CLAUDE.md is a symlink to AGENTS.md
# so Claude Code, Codex CLI, Cursor, etc. all pick up the same source of truth)
AGENTS.md
CLAUDE.md
.claude/
PHASE_NOTES.md
PARKING.md
communication.md
BALL_TUBE_FIDELITY.md

# Polyscope / ImGui runtime UI state
.polyscope.ini
imgui.ini

# WIP local-only demo (not yet ready to track)
tests/demo_phase3_torus_tube_interp.cpp

# VSCode "Local History" extension snapshots
.history/

# Stray xelatex compile log if someone runs xelatex from repo root
texput.log

# Local-only: mirrors milestone.html -> coherentmonkey.github.io/184/project/
sync_milestone.sh
